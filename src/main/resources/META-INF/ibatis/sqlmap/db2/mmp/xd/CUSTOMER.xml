<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="CUSTOMER">

    <sql id="queryDynSql">
        <isNotEmpty prepend="AND" property="CUSTOMER_ID">
            T.CUSTOMER_ID = #CUSTOMER_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="CUSTOMER_TYPE">
            T.CUSTOMER_TYPE = #CUSTOMER_TYPE#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="CUSTOMER_NAME">
            T.CUSTOMER_NAME = #CUSTOMER_NAME#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="CREATE_TIME">
            T.CREATE_TIME = #CREATE_TIME#
        </isNotEmpty>
    </sql>

    <sql id="coreSql">
        SELECT T.* FROM ${schema.xd}.CUSTOMER T
        <dynamic prepend="WHERE">
            <include refid="queryDynSql"/>
        </dynamic>
        <![CDATA[ ORDER BY T.CUSTOMER_ID]]>
    </sql>

    <select id="pageQuery" parameterClass="map" resultClass="java.util.HashMap">
        <include refid="public.pageBegin"/>
        <include refid="coreSql"/>
        <include refid="public.pageEnd"/>
    </select>

    <select id="pageCount" parameterClass="map" resultClass="int">
        <![CDATA[
			SELECT COUNT('') CNT
			FROM ${schema.xd}.CUSTOMER T
		]]>
        <dynamic prepend="WHERE">
            <include refid="queryDynSql"/>
        </dynamic>
    </select>

    <insert id="insert" parameterClass="map">
        INSERT INTO ${schema.xd}.CUSTOMER
        (
        CUSTOMER_ID
        <isNotEmpty prepend="," property="CUSTOMER_TYPE">CUSTOMER_TYPE</isNotEmpty>
        <isNotEmpty prepend="," property="CUSTOMER_NAME">CUSTOMER_NAME</isNotEmpty>
        <isNotEmpty prepend="," property="CREATE_TIME">CREATE_TIME</isNotEmpty>
        ) VALUES (
        #CUSTOMER_ID#
        <isNotEmpty prepend="," property="CUSTOMER_TYPE">#CUSTOMER_TYPE#</isNotEmpty>
        <isNotEmpty prepend="," property="CUSTOMER_NAME">#CUSTOMER_NAME#</isNotEmpty>
        <isNotEmpty prepend="," property="CREATE_TIME">#CREATE_TIME#</isNotEmpty>
        )
    </insert>

    <update id="updateById" parameterClass="map">
        <![CDATA[ update ${schema.xd}.CUSTOMER ]]>
        <dynamic prepend="set">
            <isNotNull prepend="," property="CUSTOMER_TYPE">CUSTOMER_TYPE = #CUSTOMER_TYPE#</isNotNull>
            <isNotNull prepend="," property="CUSTOMER_NAME">CUSTOMER_NAME = #CUSTOMER_NAME#</isNotNull>
            <isNotNull prepend="," property="CREATE_TIME">CREATE_TIME = #CREATE_TIME#</isNotNull>
        </dynamic>
        <![CDATA[ where CUSTOMER_ID = #CUSTOMER_ID# ]]>
    </update>

    <delete id="delete" parameterClass="map">
        <![CDATA[
         delete from ${schema.xd}.CUSTOMER where CUSTOMER_ID = #CUSTOMER_ID#
        ]]>
    </delete>

    <select id="query" parameterClass="map" resultClass="java.util.HashMap">
        <include refid="coreSql"/>
    </select>

    <select id="seq" resultClass="String">
        select ${schema.xd}.SEQ_CUSTOMER.nextval from dual
    </select>

</sqlMap>

