<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="CUSTOMER_CERTIFICATE">

    <sql id="queryDynSql">
        <isNotEmpty prepend="AND" property="CUSTOMER_CERTIFICATE_ID">
            T.CUSTOMER_CERTIFICATE_ID = #CUSTOMER_CERTIFICATE_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="CUSTOMER_ID">
            T.CUSTOMER_ID = #CUSTOMER_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="CUSTOMER_CERTIFICATE_TYPE">
            T.CUSTOMER_CERTIFICATE_TYPE = #CUSTOMER_CERTIFICATE_TYPE#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="CUSTOMER_CERTIFICATE_NO">
            T.CUSTOMER_CERTIFICATE_NO = #CUSTOMER_CERTIFICATE_NO#
        </isNotEmpty>
    </sql>

    <sql id="coreSql">
        SELECT T.* FROM ${schema.xd}.CUSTOMER_CERTIFICATE T
        <dynamic prepend="WHERE">
            <include refid="queryDynSql"/>
        </dynamic>
        <![CDATA[ ORDER BY T.CUSTOMER_CERTIFICATE_ID]]>
    </sql>

    <select id="pageQuery" parameterClass="map" resultClass="java.util.HashMap">
        <include refid="public.pageBegin"/>
        <include refid="coreSql"/>
        <include refid="public.pageEnd"/>
    </select>

    <select id="pageCount" parameterClass="map" resultClass="int">
        <![CDATA[
			SELECT COUNT('') CNT
			FROM ${schema.xd}.CUSTOMER_CERTIFICATE T
		]]>
        <dynamic prepend="WHERE">
            <include refid="queryDynSql"/>
        </dynamic>
    </select>

    <insert id="insert" parameterClass="map">
        INSERT INTO ${schema.xd}.CUSTOMER_CERTIFICATE
        (
        CUSTOMER_CERTIFICATE_ID
        <isNotEmpty prepend="," property="CUSTOMER_ID">CUSTOMER_ID</isNotEmpty>
        <isNotEmpty prepend="," property="CUSTOMER_CERTIFICATE_TYPE">CUSTOMER_CERTIFICATE_TYPE</isNotEmpty>
        <isNotEmpty prepend="," property="CUSTOMER_CERTIFICATE_NO">CUSTOMER_CERTIFICATE_NO</isNotEmpty>
        ) VALUES (
        #CUSTOMER_CERTIFICATE_ID#
        <isNotEmpty prepend="," property="CUSTOMER_ID">#CUSTOMER_ID#</isNotEmpty>
        <isNotEmpty prepend="," property="CUSTOMER_CERTIFICATE_TYPE">#CUSTOMER_CERTIFICATE_TYPE#</isNotEmpty>
        <isNotEmpty prepend="," property="CUSTOMER_CERTIFICATE_NO">#CUSTOMER_CERTIFICATE_NO#</isNotEmpty>
        )
    </insert>

    <update id="updateById" parameterClass="map">
        <![CDATA[ update ${schema.xd}.CUSTOMER_CERTIFICATE ]]>
        <dynamic prepend="set">
            <isNotNull prepend="," property="CUSTOMER_ID">CUSTOMER_ID = #CUSTOMER_ID#</isNotNull>
            <isNotNull prepend="," property="CUSTOMER_CERTIFICATE_TYPE">CUSTOMER_CERTIFICATE_TYPE = #CUSTOMER_CERTIFICATE_TYPE#</isNotNull>
            <isNotNull prepend="," property="CUSTOMER_CERTIFICATE_NO">CUSTOMER_CERTIFICATE_NO = #CUSTOMER_CERTIFICATE_NO#</isNotNull>
        </dynamic>
        <![CDATA[ where CUSTOMER_CERTIFICATE_ID = #CUSTOMER_CERTIFICATE_ID# ]]>
    </update>

    <delete id="delete" parameterClass="map">
        <![CDATA[
         delete from ${schema.xd}.CUSTOMER_CERTIFICATE where CUSTOMER_CERTIFICATE_ID = #CUSTOMER_CERTIFICATE_ID#
        ]]>
    </delete>

    <select id="query" parameterClass="map" resultClass="java.util.HashMap">
        <include refid="coreSql"/>
    </select>

    <select id="seq" resultClass="String">
        select ${schema.xd}.SEQ_CUSTOMER_CERTIFICATE.nextval from dual
    </select>

</sqlMap>

