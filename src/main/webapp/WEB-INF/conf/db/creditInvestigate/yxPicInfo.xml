<?xml version="1.0" encoding="utf-8"?>
<trans name="downPicList" ibatisStatement="" desc="影像信息">
	<!-- 请求报文，可进行必需项检查，设置缺省值，以及进行字段转换-->
	<snd>
		<!-- 客户信息  -->
		 <field  name="controlFlag"  length="30"  type="C"  desc="是否分页"/>
		  <field  name="applySerialNo"  length="30"  type="C"  desc="业务编号"/>
		   <field  name="userId"  length="30"  type="C"  desc="用户名"/>
	</snd>
	<!-- 响应报文，可过滤响应信息，并对数据字典进行解析-->
	<rcv>
		<!--公共字段 -->
		<field name="STATUS" length="6" type="C" desc="返回码" />
		<field name="MSG" length="100" type="C" desc="返回消息" />
		 <!-- 代办记录 -->
        <field-list name="LIST" type="E">
            <field name="objectCode" length="20" type="C" desc="影像资料base64图片" />
        </field-list>
	</rcv>
</trans>