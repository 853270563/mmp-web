<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="PushProcessAct">

 <select id="queryProcId" resultType="map" parameterType="map">
        select <include refid="BaseColumnList2" />
        from <include refid="TableClause" />
        where PROCESS_INST_ID=#{PROCESS_INST_ID}
        order by AUDIT_TIME 
        
    </select>
</mapper>
