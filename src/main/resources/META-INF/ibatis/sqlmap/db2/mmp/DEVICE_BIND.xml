<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="DEVICE_BIND">

    <sql id="queryDynSql">
        <isNotEmpty prepend="AND" property="BIND_ID">
            T.BIND_ID = #BIND_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="DEVICE_ID">
            T.DEVICE_ID = #DEVICE_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="APP_ID">
            T.APP_ID = #APP_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="USER_ID">
            T.USER_ID = #USER_ID#
        </isNotEmpty>
        <isNotEmpty prepend="AND" property="APP_NAME">
            T.APP_ID = #APP_ID#
        </isNotEmpty>
    </sql>

    <!-- 查询SQL -->
    <sql id="coreSql">
        SELECT T.* FROM ${schema.interPlat}.DEVICE_BIND T
        <dynamic prepend="WHERE">
            <include refid="queryDynSql"/>
        </dynamic>
        <![CDATA[ ORDER BY T.BIND_ID]]>
    </sql>
    <!-- 分页查询 -->
    <select id="pageQuery" parameterClass="map" resultClass="java.util.HashMap">
        <include refid="public.pageBegin"/>
        <include refid="coreSql"/>
        <include refid="public.pageEnd"/>
    </select>
    <!-- 分页计数 -->
    <select id="pageCount" parameterClass="map" resultClass="int">
        <![CDATA[
			SELECT COUNT('') CNT
			FROM ${schema.interPlat}.DEVICE_BIND T
		]]>
        <dynamic prepend="WHERE">
            <include refid="queryDynSql"/>
        </dynamic>
    </select>

    <!-- 增加、修改时将信息存放到op表中 -->
    <insert id="insert" parameterClass="map">
        INSERT INTO ${schema.interPlat}.DEVICE_BIND
        (
        BIND_ID
        <isNotEmpty prepend="," property="DEVICE_ID">DEVICE_ID</isNotEmpty>
        <isNotEmpty prepend="," property="APP_ID">APP_ID</isNotEmpty>
        <isNotEmpty prepend="," property="USER_ID">USER_ID</isNotEmpty>
        <isNotEmpty prepend="," property="APP_NAME">APP_NAME</isNotEmpty>
        ) VALUES (
        #BIND_ID#
        <isNotEmpty prepend="," property="DEVICE_ID">#DEVICE_ID#</isNotEmpty>
        <isNotEmpty prepend="," property="APP_ID">#APP_ID#</isNotEmpty>
        <isNotEmpty prepend="," property="USER_ID">#USER_ID#</isNotEmpty>
        <isNotEmpty prepend="," property="APP_NAME">#APP_NAME#</isNotEmpty>
        )
    </insert>

    <update id="updateById" parameterClass="map">
        <![CDATA[ update ${schema.interPlat}.DEVICE_BIND ]]>
        <dynamic prepend="set">
            <isNotNull prepend="," property="DEVICE_ID">DEVICE_ID = #DEVICE_ID#</isNotNull>
            <isNotNull prepend="," property="APP_ID">APP_ID = #APP_ID#</isNotNull>
            <isNotNull prepend="," property="USER_ID">USER_ID = #USER_ID#</isNotNull>
            <isNotNull prepend="," property="APP_NAME">APP_NAME = #APP_NAME#</isNotNull>
        </dynamic>
        <![CDATA[ where BIND_ID = #BIND_ID# ]]>
    </update>

    <delete id="delete" parameterClass="map">
        <![CDATA[

         delete from ${schema.interPlat}.DEVICE_BIND where DEVICE_ID = #DEVICE_ID#

        ]]>
    </delete>

    <select id="query" parameterClass="map" resultClass="java.util.HashMap">
        <include refid="coreSql"/>
    </select>

    <select id="seq" resultClass="String">
        select ${schema.interPlat}.SEQ_DEVICE_BIND.nextval from dual
    </select>

</sqlMap>

